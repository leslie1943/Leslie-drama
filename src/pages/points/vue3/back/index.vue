<template>
  <!--  -->
  <div></div>
  <!-- <el-button @click="increament">Count is: {{count}}, double is {{double}}, click to increament</el-button> -->
</template>

<script>
/* eslint-disable */
// 定义API
/**
 * 🚀🚀🚀🚀 导入所需的函数. 🚀🚀🚀🚀
 * ref: 创建响应性引用. (定义函数的变量.)
 * computed: 创建计算属性.
 * onMounted: 访问挂载的生命周期的hook.
 */
// import { ref, computed, onMounted } from 'vue'

// export default {
/**
 * setup:将属性和函数返回到模板的函数.
 * 在这里声明所有响应属性,计算属性,观察者和生命周期hooks,然后返回它们,以便在模板里使用
 */
// setup () {
/**
 * 使用ref函数声明了称为count的响应属性.它可以包装任何原语或对象，并返回其响应性引用.
 * 传递的元素的值将保留在所创建引用的value属性中.
 */
// const count = ref(0)
// const double = computed(() => count.value * 2)

// function increament () {
//   count.value++
// }

// 使用 onMounted hook在组件挂载时记录了一些消息.
// onMounted(() => console.info('component mounted.'))

// 返回供模板使用.
// return {
//   count,
//   double,
//   increament
// }
//   }
// }
</script>
